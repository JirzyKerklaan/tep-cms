{% extends "../layouts/admin.twig" %}

{% block body %}
    <a href="{{  route('admin.dashboard') }}">back</a>
    &nbsp;&nbsp;&nbsp;&nbsp;
    <a href="{{ route('admin.blocks.create') }}">Add block</a>

    <p>list</p>
    {% for block in blocks %}
        <li>
            <a href="{{ route('admin.blocks.edit', block) }}">
                {{ block }}
            </a>
        </li>
    {% endfor %}
{% endblock %}