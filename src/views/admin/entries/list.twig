{% extends "../layouts/admin.twig" %}

{% block body %}
    <p>list</p>
    {% for entry in entries %}
        <li>
            <a href="{{ route('admin.entries.edit', collection, entry) }}">
                {{ entry }}
            </a>
        </li>
    {% endfor %}
{% endblock %}